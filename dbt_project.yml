name: 'crypto_data'
version: '1.0.0'
config-version: 2

# This name must match the name in your profiles.yml
profile: 'crypto_data_profile'

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]

models:
  crypto_data:
    staging:
      +materialized: view
    marts:
      +materialized: table